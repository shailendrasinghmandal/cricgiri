# CricGiri Delivery API — Hugging Face Space dependencies.
# NOTE: torch + torchvision are installed separately (CPU wheels) in the Dockerfile,
# so they are intentionally NOT listed here.
ultralytics
opencv-python-headless
numpy
scipy
filterpy
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.9
pydantic-settings>=2.2.0
imageio-ffmpeg>=0.5.1
